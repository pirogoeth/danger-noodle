Int x; Float pi; String yStr;

x = 11;
x = x + 1;
pi = 3.14;
print ("x=", x);
print ("pi=", pi);
print ("And then OP said, \"I fucking love cocks!\"");
print ("And now we will try to escape a space\ . How did it go?");

yStr = 'Hello';
print ("yStr=", yStr);

yStr = "Can't";
print ("yStr=", yStr);

x = 10*3 + 7;
if x > 20:
    print ("x=", x);
    yStr = "yes";
else:
   print ("x is low");
endif;

yStr = 'Can\'t wait';
print (yStr);
